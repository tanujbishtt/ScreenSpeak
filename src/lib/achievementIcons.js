import { Footprints, Flame, Skull, Sparkles, Trophy, Images } from "lucide-react"

// Maps the `icon` string on each achievement (data/achievements.js) to its
// actual component. Was duplicated in AchievementToast.jsx and
// ProfilePage.jsx (and a since-removed AchievementShelf.jsx) — one copy now.
export const ACHIEVEMENT_ICONS = { Footprints, Flame, Skull, Sparkles, Trophy, Images }